# Secrets — NEVER commit
API.md
.env
*.key

# Python
__pycache__/
*.pyc
.venv/
venv/

# Models (large binaries — never commit)
models/
*.gguf

# Transient
data/fetch.log
.DS_Store
models

# macOS/cloud sync conflict copies
* [0-9].*
* copy.*

# Hackathon planning (not production code)
PLAN.md
DEMO_SCRIPT.md
SUBMISSION_FORM.md
